package http;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

public class HttpResponseTest extends Thread {
    private static final Logger log = LoggerFactory.getLogger(HttpResponseTest.class);
}
